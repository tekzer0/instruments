# Copy to .env and fill in. Blank values degrade gracefully (alerts print to stdout).
# NEVER put real values in this file — .env is gitignored, this file is not.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=          # your numeric user id (message @userinfobot), NOT the bot's @name
SOLDCOMPS_API_KEY=         # free key: sold-comps.com
SOLDCOMPS_URL=https://api.sold-comps.com/v1/scrape
DB_PATH=data/instruments.db
SITE_DIR=site
